require 'spec_helper'

describe Translatable do
  it 'has a version number' do
    expect(Translatable::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
